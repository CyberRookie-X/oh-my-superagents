# MT 横向评测评分标准

## 概述

本文档定义了 oh-my-superagents 项目多模型横向评测（MT）的完整评分框架。评测采用 **LLM-as-Judge（裁判模型）** 方式进行，由独立的裁判模型根据本评分标准，对被评测模型在 4 个任务中的产出进行量化评分。

### 评测范围

- **测试对象**：不同厂商的大语言模型
- **任务数量**：4 个递进式任务，覆盖分析→设计→编码→文档的完整软件工程生命周期
- **评分方式**：裁判模型（独立于被评测模型的第三方 LLM）按 Rubric 打分
- **评分等级**：每个维度 1-5 分，有明确的行为锚定描述

---

## 评分等级锚定（通用）

所有维度统一使用以下 5 级评分标准：

| 分数 | 等级 | 通用描述 |
|------|------|----------|
| 5 | 优秀 | 完全达到预期，甚至有超出预期的亮点 |
| 4 | 良好 | 基本达到预期，存在轻微不足但不影响整体质量 |
| 3 | 合格 | 勉强达到预期，有明显可改进之处 |
| 2 | 不足 | 部分达到预期，存在较大缺陷或遗漏 |
| 1 | 很差 | 几乎未达到预期，有严重错误或关键内容缺失 |

---

## 总分计算公式

```
最终得分 = 0.20 × S1 + 0.25 × S2 + 0.50 × S3 + 0.05 × S4
```

### 权重设计说明

| 任务 | 权重 | 设计理由 |
|------|------|----------|
| Task 1（架构分析） | 20% | 考验模型对复杂项目的理解能力，是后续任务的基础 |
| Task 2（设计文档） | 25% | 考验模型的规划与架构设计能力，设计质量直接决定实施成败 |
| Task 3（实施开发） | 50% | 核心任务，工作量最大、复杂度最高，最能体现真实软件工程能力 |
| Task 4（文档完善） | 5% | 考验模型的文档编写与项目收尾能力，权重较低因更侧重开发能力 |

---

## Task 1：架构分析报告（权重 20%）

**任务描述**：深入分析 oh-my-superagents 项目的代码架构与设计，输出一份中文分析报告 `analysis-report.md` 放到根目录。

### 评分维度

| 维度 | 权重 | 说明 |
|------|------|------|
| 架构准确性 | 35% | 对项目分层架构、模块关系、核心流程的理解是否正确？有无事实性错误？ |
| 分析深度 | 30% | 是否超越表面描述，深入分析了设计决策、技术选型、架构演进脉络？有无独到见解？ |
| 报告完整度 | 20% | 是否覆盖了项目所有关键部分（7 层架构、host adapters、workflow sources、context 子系统、policy 子系统等）？ |
| 结构清晰度 | 15% | 报告结构是否逻辑清晰、层次分明、易于阅读？语言表达是否专业准确？ |

### Rubric 锚定

#### 架构准确性（权重 35%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 对项目 7 层架构理解完全正确，各层职责、模块间关系、数据流方向描述准确无误。对 control-plane、router、host adapters、workflow sources 等核心概念的定义和使用与源码一致。无任何事实性错误。 |
| 4 | 对主要架构理解正确，核心模块关系描述准确。可能有 1-2 处次要细节不够精确（如某个子模块归属层级有偏差），但不影响整体理解的正确性。 |
| 3 | 对架构的整体理解基本正确，但存在 3-5 处明显的事实性错误或重要遗漏。例如：混淆了某两个层级的职责，或错误描述了某个 adapter 的工作方式。 |
| 2 | 对架构理解存在多处重大错误（5 处以上），或对核心概念（如 routing、host adapter）的理解有根本性偏差。报告中有大段不准确的内容。 |
| 1 | 对项目架构几乎完全误解，或报告充斥着错误信息。可能将项目描述成了完全不同的东西。 |

#### 分析深度（权重 30%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 不仅描述了"是什么"，还深入分析了"为什么"。对关键设计决策（如：为什么 7 层架构这样分层？为什么 shared core 要 thicker than any host adapter？）有深入解读。能识别出架构的 trade-off 和潜在演进方向。有原创性的洞察。 |
| 4 | 在描述架构的基础上，对部分关键设计决策有分析和解读。能解释一些"为什么这样做"的问题，但深度和原创性不如 5 分。 |
| 3 | 以描述为主，分析较少。主要回答"是什么"，对"为什么"的分析较浅或只有少量提及。报告更像一份目录清单而非深度分析。 |
| 2 | 几乎只有表面描述，缺乏分析。例如：仅列出了文件列表和模块名称，没有对设计思想的理解。 |
| 1 | 完全没有分析，或分析内容与项目无关。可能只是对 README 的简单复述。 |

#### 报告完整度（权重 20%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 全面覆盖项目所有关键部分：7 层架构（control-plane、workflow adapters、capability policy、host adapters、compatibility monitor、artifact reconciliation、docs & plans）、4 个 host adapters、3 个 workflow sources、context 子系统、policy 子系统、CLI 命令体系。无明显遗漏。 |
| 4 | 覆盖了大部分关键部分，可能有 1-2 个次要子系统（如 context-provider-cli、gstack-detectors）未提及或一笔带过。 |
| 3 | 覆盖了主要架构和核心模块，但遗漏了 3-4 个重要部分（如未提及 compatibility monitor、未分析 workflow sources）。 |
| 2 | 仅覆盖了少量关键部分，大量重要子系统缺失。报告内容明显不完整。 |
| 1 | 报告极其简略，几乎没有覆盖项目核心内容。可能只写了几段泛泛而谈的内容。 |

#### 结构清晰度（权重 15%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 报告结构逻辑清晰：有明确的层次结构、合理的章节划分、清晰的目录。行文流畅，专业术语使用准确，中文表达规范。配合图表（如架构图、流程图）辅助说明。 |
| 4 | 结构合理，章节划分清晰，行文通顺。可能在部分章节的组织上略显松散，或缺少图表辅助。 |
| 3 | 结构基本可读，但组织方式不够理想。例如：章节划分不合理、前后逻辑跳跃、缺少过渡。 |
| 2 | 结构混乱，难以跟随作者的思路。段落之间缺乏逻辑关联，读者需要自己梳理信息。 |
| 1 | 完全没有结构可言，内容散乱无章，或大量使用无意义的填充内容。 |

### Task 1 得分计算

```
S1 = 0.35 × 架构准确性 + 0.30 × 分析深度 + 0.20 × 报告完整度 + 0.15 × 结构清晰度
```

---

## Task 2：设计文档（权重 25%）

**任务描述**：采用 superpowers 工作流，完成"用 Agent + Plugin + Hooks 方案实现完整的 Copilot CLI 宿主深度支持"开发所需的全部文档。

### 评分维度

| 维度 | 权重 | 说明 |
|------|------|------|
| 文档完整度 | 30% | 是否产出了全套文档（spec 设计文档 + plan 实施计划）？是否覆盖了 Agent、Plugin、Hooks 三个层面？是否包含与现有 7 层架构的融合方案？ |
| 设计合理性 | 30% | Agent/Plugin/Hooks 方案是否架构合理？是否遵循了项目现有的 host adapter 模式？接口定义是否清晰？ |
| Workflow 遵循度 | 25% | 是否严格遵循 superpowers 工作流（brainstorming → design → plan）？spec 文档是否通过了自我审查？ |
| 可行性与可操作性 | 15% | 设计方案是否具体可落地？Plan 中的步骤是否清晰、有明确的验收标准？ |

### Rubric 锚定

#### 文档完整度（权重 30%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 产出了完整的 spec 设计文档和 plan 实施计划。文档覆盖了 Agent（host-native agent 生成方案）、Plugin（插件入口与引导方案）、Hooks（宿主钩子集成方案）三个层面。包含与现有 7 层架构的融合方案（新增 host adapter 层如何插入、compatibility monitor 如何注册、materialize 如何适配）。所有文档内容充实、细节到位。 |
| 4 | 产出了 spec 和 plan 两份文档，覆盖了 Agent、Plugin、Hooks 三个层面。在架构融合方案的某些细节上可能不够深入。 |
| 3 | 产出了主要文档，但存在明显遗漏。例如：缺少 plan 文档、或未覆盖 Hooks 方案、或架构融合方案过于粗略。 |
| 2 | 文档严重不完整。可能只产出了一份文档，或三层面中有 1-2 个完全缺失。 |
| 1 | 几乎没有产出有效文档，或内容与任务要求完全不符。 |

#### 设计合理性（权重 30%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 设计方案完全遵循项目现有 host adapter 模式（参考 `src/opencode.ts`、`src/codex.ts`、`src/qwen.ts`、`src/claude.ts`）。Agent 生成方案复用了 `materialize.ts` 的产物写入机制。Plugin 方案合理利用了宿主原生插件系统。Hooks 方案清晰定义了钩子点和数据流。接口定义明确，与现有共享层（capabilities、router、control-plane）的对接方案合理。 |
| 4 | 设计方案大体遵循现有模式，有 1-2 处细节与现有模式存在偏差（如 agent 生成格式与现有 adapter 不完全一致），但不影响整体合理性。 |
| 3 | 设计方案有基本合理性，但存在 3-5 处与现有模式不符之处，或接口定义不够清晰，导致后续实现可能有歧义。 |
| 2 | 设计方案存在重大架构问题。例如：未复用共享层能力、自行实现了一套不兼容的机制、与现有架构存在明显冲突。 |
| 1 | 设计方案完全不考虑现有架构，或方案本身在技术上不可行。 |

#### Workflow 遵循度（权重 25%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 严格遵循 superpowers 工作流：先 brainstorming 探索需求，再输出 design spec，最后产出 implementation plan。spec 文档中有明确的自我审查痕迹（如 checking for TBD、内部一致性验证、scope check）。文档存放在正确的路径（`docs/superpowers/specs/` 和 `docs/superpowers/plans/`）。 |
| 4 | 遵循了主要工作流步骤，但某个环节不够严格。例如：brainstorming 痕迹不够明显、自我审查不完整。 |
| 3 | 大致遵循工作流，但跳过了某个重要环节（如直接写 plan 没有 spec），或文档存放路径不符合项目规范。 |
| 2 | 工作流遵循度很差，几乎看不到 brainstorming/design/plan 的递进过程。 |
| 1 | 完全没有遵循 superpowers 工作流。 |

#### 可行性与可操作性（权重 15%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 设计方案具体可落地。Plan 中的每个步骤都有清晰的输入、输出和验收标准。实施步骤的粒度和顺序合理。考虑了边界情况和错误处理。Task 3 可以完全依照此 plan 实施，无需额外澄清。 |
| 4 | 方案可落地，plan 步骤基本清晰。可能有 1-2 个步骤的验收标准不够具体。 |
| 3 | 方案大致可行，但 plan 中有多处模糊不清的地方。实施者在执行时可能需要自行判断和补充。 |
| 2 | 方案存在明显的可行性问题。plan 步骤过于粗略（如"实现 Copilot CLI 支持"作为一个步骤），无法指导实际开发。 |
| 1 | 方案不可落地，或 plan 完全无法执行。 |

### Task 2 得分计算

```
S2 = 0.30 × 文档完整度 + 0.30 × 设计合理性 + 0.25 × Workflow遵循度 + 0.15 × 可行性与可操作性
```

---

## Task 3：实施开发（权重 50%）

**任务描述**：实施 Task 2 中计划的所有开发工作，完成"用 Agent + Plugin + Hooks 方案实现完整的 Copilot CLI 宿主深度支持"。采用 superpowers 的 TDD 等一系列 workflow，以 subagent-driven-development 模式进行，不在主 agent 中直接进行开发。

### 评分设计原则

**S3 只考察执行力**：设计的正确性、广度、深度已在 Task 2（S2）中充分评估。S3 仅评估「是否按 S2 的 spec/plan 写出了好代码」，不重复评估设计质量。这避免了"一个设计错误、双重扣分"的评分缺陷。

### 评分维度

| 维度 | 权重 | 说明 |
|------|------|------|
| 设计-实现一致性 | 40% | 是否按**自己的** Task 2 spec/plan 完成了功能？即使设计方向有偏差，在自己框架内是否执行到位？ |
| 代码质量 | 30% | TypeScript 类型安全、风格一致性、错误处理、是否遵循项目现有 host adapter 模式？ |
| TDD 与开发流程 | 20% | 是否有测试？测试覆盖是否充分？commit 历史是否清晰渐进（红→绿→重构）？ |
| 自洽性 | 10% | 实现内部逻辑是否一致、可编译运行？产出的代码和文档之间是否自洽？ |

### Rubric 锚定

#### 设计-实现一致性（权重 40%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 完整实现了自己 spec/plan 中规划的所有功能点。产物结构、文件路径、接口签名与设计文档高度一致。 |
| 4 | 实现了 spec/plan 中规划的大部分功能。可能有 1-2 个次要功能点未实现或与设计有出入。 |
| 3 | 实现了约一半的规划功能，或实现与设计存在明显偏差（如设计规划了 hooks.json 但未生成）。 |
| 2 | 仅实现了少量规划功能。实现与设计严重脱节。 |
| 1 | 实现与设计完全无关，或无设计文档可参照。 |

#### 代码质量（权重 30%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 代码结构清晰、命名规范。完全遵循项目 host adapter 模式。合理的错误处理。TypeScript 类型恰当。无代码坏味道。 |
| 4 | 代码整体质量好，基本遵循项目模式。可能有 1-2 处风格不一致。 |
| 3 | 代码可读但存在多处质量问题：函数过长、缺少错误处理、类型不完整。 |
| 2 | 代码质量较差：大量重复代码、逻辑混乱、类型使用不当。 |
| 1 | 代码质量极差：不可维护，有逻辑错误或安全风险。 |

#### TDD 与开发流程（权重 20%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 严格遵循 TDD：commit 历史显示红→绿→重构。测试文件遵循 vitest 风格。测试覆盖核心功能、边界情况、错误路径。测试命名清晰、结构合理。 |
| 4 | 遵循 TDD 主要流程。测试覆盖核心功能和主要边界情况。红-绿-重构分离不够清晰但整体流程合理。 |
| 3 | 有测试但不严格：测试与实现一起提交，或测试在实现之后补写。覆盖不够全面。 |
| 2 | 测试很少或质量差。 |
| 1 | 完全没有测试。 |

#### 自洽性（权重 10%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 代码可编译通过，逻辑内部一致。产物文件之间引用正确（如 agent 文件引用的 skill 路径存在）。无矛盾或死代码。 |
| 4 | 基本自洽，可能有 1 处轻微不一致（如注释过时）。 |
| 3 | 存在多处不一致：如生成的 agent 文件引用了不存在的 skill，或部分代码路径不可达。 |
| 2 | 严重不一致：代码无法编译，或核心功能路径断裂。 |
| 1 | 完全不自洽：代码无法运行，逻辑矛盾。 |

### Task 3 得分计算

```
S3 = 0.40 × 设计实现一致性 + 0.30 × 代码质量 + 0.20 × TDD与开发流程 + 0.10 × 自洽性
```

---

## Task 4：文档完善（权重 5%）

**任务描述**：完善并更新项目文档。

### 评分维度

| 维度 | 权重 | 说明 |
|------|------|------|
| 更新完整度 | 40% | 是否更新了所有相关文档？包括 README（中英文）、架构文档、AI 文档、capability catalog、onboarding playbook 等。 |
| 内容准确性 | 35% | 新增/修改的文档内容是否准确？技术描述是否与实际实现一致？示例是否可运行？ |
| 可读性与一致性 | 25% | 文档风格是否与现有文档一致？语言表达是否清晰？是否遵循了项目文档的结构模式？ |

### Rubric 锚定

#### 更新完整度（权重 40%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 全面更新了所有相关文档：README.md 和 README.zh-CN.md 中增加了 Copilot CLI 的支持状态；架构文档（docs/README-architecture.md 及其中文版）更新了 host adapter 列表和架构图；AI 文档（docs/ai/llms.txt、oms-capability-catalog.md、oms-onboarding-playbooks.md）反映了新增的 host 支持。所有需要更新的文档都得到了更新。 |
| 4 | 更新了主要文档（README、架构文档），可能有 1-2 个次要文档（如 AI 文档目录）未更新。 |
| 3 | 更新了 README 和部分架构文档，但遗漏了 3-4 个需要更新的文档。 |
| 2 | 只更新了极少量文档（如仅修改了 README 一行），大量相关文档未动。 |
| 1 | 几乎没有更新任何文档，或更新的内容与任务无关。 |

#### 内容准确性（权重 35%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 所有文档更新内容准确无误。技术描述与实际实现一致。文档中的命令示例、配置示例、代码片段均可运行。host 支持状态、功能列表等信息与实现完全匹配。 |
| 4 | 文档内容基本准确，可能有 1-2 处小错误（如示例中的参数名与实际略有不同）。 |
| 3 | 文档有多处不准确（3-5 处）：如功能描述与实际实现不符、示例代码不可运行、状态信息过时。 |
| 2 | 文档存在大量错误或误导性信息。 |
| 1 | 文档内容与实际实现严重不符，几乎全是错误信息。 |

#### 可读性与一致性（权重 25%）

| 分数 | 锚定描述 |
|------|----------|
| 5 | 文档风格与现有文档完全一致。英文版和中文版保持了项目一贯的写作风格和排版规范。新增内容语言清晰、表达专业。遵循了项目文档的结构模式（如英文版+中文版配对、AI 可读文档格式）。 |
| 4 | 风格基本一致，可能在个别段落的语气或格式上有轻微差异。 |
| 3 | 风格存在多处不一致：如中文版和英文版内容不对应、排版格式与现有文档不同、语言风格突变。 |
| 2 | 风格严重不一致，阅读体验差。 |
| 1 | 文档质量极差：语言不通顺、格式混乱、与现有文档风格完全不同。 |

### Task 4 得分计算

```
S4 = 0.40 × 更新完整度 + 0.35 × 内容准确性 + 0.25 × 可读性与一致性
```

---

## 裁判模型评测 Prompt 模板

以下为裁判模型打分时的标准 prompt 模板。对每个被评测模型，按 Task 分别评分。

### 通用评测 Prompt

```
你是一个软件工程评测专家。你需要根据给定的评分标准（Rubric），对被评测模型在某个任务上的产出进行评分。

## 评分规则
- 每个维度评 1-5 分，分数含义见评分等级锚定
- 你必须为每个分数提供具体、基于证据的理由
- 理由必须引用被评测模型产出的具体内容（文件名、行号、代码片段、commit hash 等）
- 如果某个维度的证据不足，明确说明，不能凭空猜测

## 评分等级锚定
| 分数 | 等级 | 通用描述 |
|------|------|----------|
| 5 | 优秀 | 完全达到预期，甚至有超出预期的亮点 |
| 4 | 良好 | 基本达到预期，存在轻微不足但不影响整体质量 |
| 3 | 合格 | 勉强达到预期，有明显可改进之处 |
| 2 | 不足 | 部分达到预期，存在较大缺陷或遗漏 |
| 1 | 很差 | 几乎未达到预期，有严重错误或关键内容缺失 |

## 被评测模型信息
- 模型标识：[MODEL_ID]
- 工作分支：[BRANCH_NAME]

## 任务 X 评分标准
[粘贴对应 Task 的 Rubric 内容]

## 评分输出格式
请严格按照以下 JSON 格式输出评分结果：

```json
{
  "task": "Task X",
  "model": "[MODEL_ID]",
  "dimensions": {
    "维度1名称": {
      "score": 1-5,
      "weight": 0.XX,
      "reason": "基于证据的打分理由"
    },
    "维度2名称": {
      "score": 1-5,
      "weight": 0.XX,
      "reason": "基于证据的打分理由"
    }
  },
  "total_score": 加权计算后的任务总分,
  "summary": "对该模型在本任务上表现的总体评价（100字以内）"
}
```
```

---

## 评测流程

1. **环境准备**：为每个被评测模型准备独立的工作分支，包含其完整的产出（文件、commit 历史）
2. **逐任务评测**：裁判模型分别对每个 Task 按 Rubric 独立评分
3. **交叉验证**：每个 Task 至少由裁判模型评分 2 次（不同 session），取平均分以减少随机性
4. **总分汇总**：按加权公式计算最终得分
5. **排名输出**：按最终得分从高到低排列各模型，附各任务分维度得分明细

---

## 附录：分数区间解读

| 总分区间 | 等级 | 解读 |
|----------|------|------|
| 4.5 - 5.0 | S | 在软件工程全流程中表现卓越，可作为首选模型 |
| 4.0 - 4.4 | A | 各环节表现优秀，仅有少量可改进之处 |
| 3.0 - 3.9 | B | 整体可用，但在某些环节（如 TDD、文档）有明显不足 |
| 2.0 - 2.9 | C | 勉强完成基本任务，存在较多缺陷，需要人工大量介入 |
| 1.0 - 1.9 | D | 基本无法独立完成任务，不适合用于此类软件工程场景 |
